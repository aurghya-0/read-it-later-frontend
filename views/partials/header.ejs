<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Read It Later</title>
  <link rel="stylesheet" href="/styles/styles.css">
  <link rel="stylesheet" href="/styles/tailwind.css">
  <!-- <script src="https://cdn.tailwindcss.com"></script> -->
</head>
<body class="bg-gray-100">

  <!-- Header Section -->
  <header class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex items-center justify-between">
      <h1 class="text-2xl font-bold">Read It Later</h1>
      <nav>
        <a href="/" class="<%= currentRoute === '/' ? 'underline font-bold' : '' %> text-white hover:underline">Home</a>
        <a href="/categories" class="<%= currentRoute === '/categories' ? 'underline font-bold' : '' %> text-white hover:underline ml-4">Categories</a>
        <a href="/add-article" class="<%= currentRoute === '/add-article' ? 'underline font-bold' : '' %> text-white hover:underline ml-4">Add Article</a>
      </nav>
    </div>
  </header>
